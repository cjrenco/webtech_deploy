<div class="container">
  <h1>Questions</h1>
  
  <%= link_to 'Add a Question', new_course_lesson_forum_question_path(@forum.lesson.course, @forum.lesson, @forum), class: 'btn btn-success mb-3' %>

  <ul class="list-group">
    <% @questions.each do |question| %>
      <li class="list-group-item">
        <h5 class="mb-1">"<%= question.title %>" by <%= question.utilizer.full_name %></h5>
        <p><%= question.content %></p>
        <%= link_to 'Show', course_lesson_forum_question_path(@forum.lesson.course, @forum.lesson, @forum, question), class: 'btn btn-info' %>
        <%= button_to 'Delete', course_lesson_forum_question_path(@forum.lesson.course, @forum.lesson, @forum, question), method: :delete , class: 'btn btn-danger btn-sm' , data: {turbo_confirm: 'Are you sure?'}  %>
      </li>
    <% end %>
  </ul>
</div>
